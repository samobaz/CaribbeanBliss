@{
    ViewData["Title"] = "Acceso Denegado";
    Layout = "_Layout"; // Asegúrate que use el layout correcto
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="alert alert-danger">
                <h2><i class="fas fa-exclamation-triangle"></i> Acceso Denegado</h2>
                <p class="lead">No tienes permiso para acceder a esta página.</p>
                <a href="@Url.Action("Login", "Caribbean")" class="btn btn-primary">
                    <i class="fas fa-sign-in-alt"></i> Volver al Login
                </a>
            </div>
        </div>
    </div>
</div>
